
### Task For Git/Githup Course CSE  
### Name:khalid Ahmad Dababat

   ### Theoretical Part:  

1) 

  git status:
     Shows the current state of the working directory and staging area
  git init: 
     Creates a new empty Git repository in the current directory
  git commit:
     Creates a snapshot with a message
  git push:
     Sends local commits to a remote repository (like GitHub)

2) disagree with ahmad Becouse Can't Send To remote Repostory Without Commite 

3) 
  a-  1-Git lab 2- GitHup 3- Bitbucket 

  b-   1- Store and access code online
       2- Collaboration tools
       3-Role-based permissions for teams
       4-Bug reporting, task management
       5-Automate build, test, and deployment(CI/CD integration)
       6-Edit, view, and browse code in-browser

  c- True 

4)
      -u flag >>> git push -u origin master  link Your local branch to the remote origin/master
         git push   #No need To write origin master
         git pull   # ----- Same  BEcouse -u always specify the branch  


### Practical Part:

     https://github.com/khalidDababat/TaskCSE-.git   //  HTTPS  
     git@github.com:khalidDababat/TaskCSE-.git       // SSH 
   

1- Create a Local Git Repository  
      mkdir my-project
      cd my-project
      git init
2- Create a New Repository on GitHub
3- Add GitHub as Remote Using SSH 
     git remote add origin  https://github.com/khalidDababat/TaskCSE-.git 
4- Add Files, Commit, and Push
     git add .
     git commit -m "Initial commit"
     git push -u origin master 

   I Used That During Practical Part 












     